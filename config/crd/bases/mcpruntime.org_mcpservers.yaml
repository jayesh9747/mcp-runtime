---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: mcpservers.mcpruntime.org
spec:
  group: mcpruntime.org
  names:
    kind: MCPServer
    listKind: MCPServerList
    plural: mcpservers
    singular: mcpserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .status.deploymentReady
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MCPServer is the Schema for the mcpservers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MCPServerSpec defines the desired state of MCPServer
            properties:
              envVars:
                description: EnvVars are environment variables to pass to the container
                items:
                  description: EnvVar represents an environment variable
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              image:
                description: Image is the container image for the MCP server
                type: string
              imagePullSecrets:
                description: ImagePullSecrets are secrets to use for pulling the image
                items:
                  type: string
                type: array
              imageTag:
                description: ImageTag is the tag of the container image (defaults
                  to "latest")
                type: string
              ingressAnnotations:
                additionalProperties:
                  type: string
                description: IngressAnnotations are additional annotations for the
                  ingress controller
                type: object
              ingressClass:
                description: IngressClass is the ingress class to use (e.g., "traefik",
                  "nginx", "istio"). Defaults to "traefik"
                type: string
              ingressHost:
                description: IngressHost is the hostname for the ingress (optional;
                  defaults from MCP_DEFAULT_INGRESS_HOST env var if set on the operator)
                type: string
              ingressPath:
                description: IngressPath is the path for the ingress route (defaults
                  to /{name}/mcp)
                type: string
              port:
                description: Port is the port the container listens on (defaults to
                  8088)
                format: int32
                type: integer
              registryOverride:
                description: RegistryOverride, if set, overrides the registry portion
                  of the image (e.g., registry.example.com)
                type: string
              replicas:
                description: Replicas is the number of desired replicas (defaults
                  to 1)
                format: int32
                type: integer
              resources:
                description: Resources defines resource limits and requests
                properties:
                  limits:
                    description: ResourceList defines CPU and memory resources
                    properties:
                      cpu:
                        type: string
                      memory:
                        type: string
                    type: object
                  requests:
                    description: ResourceList defines CPU and memory resources
                    properties:
                      cpu:
                        type: string
                      memory:
                        type: string
                    type: object
                type: object
              servicePort:
                description: ServicePort is the port exposed by the service (defaults
                  to 80)
                format: int32
                type: integer
              useProvisionedRegistry:
                description: UseProvisionedRegistry tells the controller to use the
                  provisioned registry (from operator env) for this server
                type: boolean
            required:
            - image
            type: object
          status:
            description: MCPServerStatus defines the observed state of MCPServer
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition represents a condition status
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              deploymentReady:
                description: DeploymentReady indicates if the deployment is ready
                type: boolean
              ingressReady:
                description: IngressReady indicates if the ingress is ready
                type: boolean
              message:
                description: Message provides additional information about the status
                type: string
              phase:
                description: Phase represents the current phase of the MCPServer
                type: string
              serviceReady:
                description: ServiceReady indicates if the service is ready
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
